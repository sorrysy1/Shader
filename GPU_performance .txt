1.哪种顶点属性数据存储方式在GPU上性能更好？
把一个顶点的所有属性放在一起（array of structures）。
其原因是每个顶点的属性数据以连续的方式读取，使内存访问效率更高。
其缺点是，如果要修改其中部分属性数据，将导致整个属性buffer全部重新装载，解决此问题的方法是把这些需要动态修改的属性数据放在单独的buffer中

2.顶点属性数据类型优化
 顶点属性数据类型不会影响在GPU上每个顶点占用的内存，但在Render a Frame时，它影响CPU与GPU之间的内存带宽。推荐尽量使用GL_HALF_FLOAT_OES。
